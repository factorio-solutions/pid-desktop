// import {}  from '../actions/admin.timeCredit.actions'


const defaultState = {
}


export default function timeCredit(state = defaultState, action) {
  switch (action.type) {

    // case ANALYTICS_SET_RESERVATIONS:
    //   return {
    //     ...state,
    //     reservations: action.value
    //   }

    default:
      return state
  }
}
